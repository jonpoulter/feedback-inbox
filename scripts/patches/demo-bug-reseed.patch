diff --git a/app/services.py b/app/services.py
index e3c6745..99ac85b 100644
--- a/app/services.py
+++ b/app/services.py
@@ -47,7 +47,7 @@ def get_stats(
     items = list_items(db, status=status, category=category)
     total = len(items)
     reviewed = sum(1 for item in items if item.status == "reviewed")
-    percent_reviewed = round(reviewed / total * 100) if total else 0
+    percent_reviewed = round(reviewed / total * 100)
     return {
         "total": total,
         "reviewed": reviewed,
diff --git a/tests/test_api.py b/tests/test_api.py
index e66ef5f..516e06d 100644
--- a/tests/test_api.py
+++ b/tests/test_api.py
@@ -1,3 +1,6 @@
+import pytest
+
+
 def test_list_items_empty(client):
     response = client.get("/api/items")
 
@@ -110,28 +113,11 @@ def test_stats_returns_counts(client):
     assert response.json() == {"total": 2, "reviewed": 1, "percent_reviewed": 50}
 
 
-def test_stats_empty_filtered_set_returns_zero_percent(client):
-    client.post(
-        "/api/items",
-        json={"title": "Only new", "body": "Body", "category": "bug"},
-    )
-
-    response = client.get("/api/stats?status=reviewed")
-
-    assert response.status_code == 200
-    assert response.json() == {"total": 0, "reviewed": 0, "percent_reviewed": 0}
-
-
-def test_list_and_stats_reviewed_filter_with_no_reviewed_items(client):
+def test_stats_empty_filtered_set_crashes(client):
     client.post(
         "/api/items",
         json={"title": "Only new", "body": "Body", "category": "bug"},
     )
 
-    items_response = client.get("/api/items?status=reviewed&category=all")
-    stats_response = client.get("/api/stats?status=reviewed&category=all")
-
-    assert items_response.status_code == 200
-    assert items_response.json() == []
-    assert stats_response.status_code == 200
-    assert stats_response.json()["percent_reviewed"] == 0
+    with pytest.raises(ZeroDivisionError):
+        client.get("/api/stats?status=reviewed")
diff --git a/tests/test_services.py b/tests/test_services.py
index 1ed91fd..cf4b98f 100644
--- a/tests/test_services.py
+++ b/tests/test_services.py
@@ -5,7 +5,6 @@ from app.schemas import FeedbackItemCreate
 from app.services import (
     InvalidCategoryFilterError,
     create_item,
-    get_stats,
     list_items,
     mark_reviewed,
 )
@@ -95,39 +94,6 @@ def test_mark_reviewed(db_session):
     assert updated.status == "reviewed"
 
 
-def test_get_stats_counts_reviewed(db_session):
-    create_item(
-        db_session,
-        FeedbackItemCreate(title="New bug", body="Body", category="bug"),
-    )
-    reviewed = create_item(
-        db_session,
-        FeedbackItemCreate(title="Reviewed bug", body="Body", category="bug"),
-    )
-    mark_reviewed(db_session, reviewed.id)
-
-    stats = get_stats(db_session)
-
-    assert stats == {"total": 2, "reviewed": 1, "percent_reviewed": 50}
-
-
-def test_get_stats_empty_set_returns_zero_percent(db_session):
-    stats = get_stats(db_session)
-
-    assert stats == {"total": 0, "reviewed": 0, "percent_reviewed": 0}
-
-
-def test_get_stats_reviewed_filter_with_no_matches(db_session):
-    create_item(
-        db_session,
-        FeedbackItemCreate(title="New only", body="Body", category="bug"),
-    )
-
-    stats = get_stats(db_session, status="reviewed")
-
-    assert stats == {"total": 0, "reviewed": 0, "percent_reviewed": 0}
-
-
 def test_create_item_rejects_whitespace_only_fields():
     with pytest.raises(ValidationError):
         FeedbackItemCreate(title="   ", body="Body", category="bug")
